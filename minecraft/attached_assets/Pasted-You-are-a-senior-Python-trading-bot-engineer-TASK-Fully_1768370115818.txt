You are a senior Python trading-bot engineer.

TASK:
Fully audit this entire project after GitHub ZIP upload.
Files and folders were moved during GitHub upload, so imports, paths, and references may be broken.

INSTRUCTIONS (DO NOT SKIP ANY STEP):

1. Scan ALL files and folders recursively.
2. Identify and FIX:
   - Broken or wrong Python imports
   - Incorrect relative / absolute file paths
   - Missing __init__.py issues
   - Circular imports between modules
   - Hardcoded paths that no longer exist
   - Any file that assumes a different directory structure

3. Verify these core files are correctly linked and communicating:
   - market_data_and_signal.py
   - trade_execution_and_mode.py
   - trade_management_and_risk.py
   - state_manager.py
   - event_logger.py
   - dashboard_api.py

4. Ensure:
   - All internal module imports work after folder relocation
   - No duplicate logic exists due to folder move
   - All JSON / state / log files are created dynamically if missing
   - Safe defaults are used if files are empty or not found

5. Web Dashboard Checks:
   - Verify dashboard_api.py endpoints match frontend calls
   - Fix missing routes like /logs/recent if frontend expects them
   - Ensure status, active trades, logs endpoints never crash
   - Return mock / placeholder data if real data is unavailable

6. Trading Logic Safety:
   - Market may be CLOSED — bot must stay IDLE without crashing
   - If no live data, bot must switch to PAPER / SIMULATION mode
   - Do NOT assume broker API or TradingView is connected

7. Logging & Visibility:
   - Add clear INFO logs on:
     - Bot startup
     - Market open / close detection
     - Data fetch attempts
     - Trade signal generation
     - Trade execution & exit
   - Logs must help identify whether bot is WORKING or WAITING

8. NO external API tokens allowed:
   - DO NOT add Telegram, broker, or TradingView tokens
   - Use environment variables only (placeholders allowed)

9. Final Step:
   - Automatically fix all discovered issues
   - Refactor imports & paths cleanly
   - Ensure project runs without errors in Replit
   - Print a final summary:
     ✔ Files fixed
     ✔ Imports corrected
     ✔ APIs working
     ✔ Bot state (IDLE / READY / RUNNING)

IMPORTANT:
Do NOT redesign logic.
Do NOT change strategy rules.
ONLY fix broken structure, imports, paths, and runtime errors.

Proceed now.
